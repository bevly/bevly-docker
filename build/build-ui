#! /bin/bash

set -e
rm -rf tmp
mkdir tmp

echo "Building UI"
( cd tmp && git clone https://github.com/bevly/bevly-ui.git )
( cd tmp/bevly-ui && ./build )
mkdir -p dist
cp -r tmp/bevly-ui/dist/app dist/bevly-ui